# Assistant IA Base

Initial repository.

## Frontend

A minimal login page scaffold is available at `frontend/src/auth/login.html`. It posts to `/api/auth/login` and stores the returned token in memory.
