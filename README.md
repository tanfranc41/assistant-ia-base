# Assistant IA Base

Initial repository.

## Backend

The backend runs on Node.js (LTS, such as 18+) and is intended to be deployed as a containerized service to a cloud platform (for example Render or Heroku). A placeholder entry point is available in `backend/index.js` until business logic is added.
