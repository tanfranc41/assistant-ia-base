# Frontend

This directory holds the future user interface for the project. It currently contains only placeholder folders for source code (`src/`) and static assets (`public/`). No framework or implementation is set up yet.
