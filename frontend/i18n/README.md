# Multilingual support scaffold

This directory hosts the multilingual scaffold for the frontend. It contains one subdirectory per supported locale (`en`, `fr`, `nl`, `de`). Add locale-specific translation files or resources inside each language folder when implementing internationalization.

No translations or functional code are included here by design. Only structure and documentation are provided to prepare for future i18n work.
