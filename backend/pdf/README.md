# PDF Generation

This directory houses the scaffolding for creating PDF outputs such as invoices, receipts, and data exports.

- `generators/`: generation orchestration and helpers
- `templates/`: layout and styling assets for PDF documents
- `services/`: integration points for triggering and delivering PDFs
